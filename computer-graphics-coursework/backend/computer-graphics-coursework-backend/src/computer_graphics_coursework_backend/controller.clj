(ns computer-graphics-coursework-backend.controller)

(defn get-time
  "Current time in millis"
  []
  (System/currentTimeMillis))


(defn process-input
  "Process all of the user actions"
  [])

(defn is-close-requested
  "Is there an user interrupt?"
  [])

